[build]
builder = "RAILPACK"
buildCommand = "pnpm --filter @dev-event/frontend build"

[deploy]
startCommand = "pnpm --filter @dev-event/frontend start"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
