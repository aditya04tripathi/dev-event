[build]
builder = "RAILPACK"
buildCommand = "pnpm --filter @dev-event/backend build"

[deploy]
startCommand = "pnpm --filter @dev-event/backend start:prod"
healthcheckPath = "/health/liveness"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
